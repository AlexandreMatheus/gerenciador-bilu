import React, { useEffect, useState } from 'react';

type VisualizarCompraProps = {
  orderId: number | null;
  onBack: () => void;
};

const VisualizarCompra: React.FC<VisualizarCompraProps> = ({ orderId, onBack }) => {
 return (<></>)
};

export default VisualizarCompra;